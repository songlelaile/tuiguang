# 货盘分析 BI 工具逻辑梳理 - Web 端展示口径

## 1. 这个 BI 的本质

这个工具不是单张报表，而是一个“源数据导入 -> 字段清洗/拼接 -> 上下合并 -> 分类汇总 -> 派生指标 -> 图表/明细表展示”的货盘经营分析看板。

核心目标有两层：

1. **商品经营层**：用生意参谋商品维度数据，看每个商品的成交规模、转化、退款、复购、加购、推广费比和净 ROI。
2. **推广投放层**：用推广商品、内容、关键词、人群报表，看不同货品、计划、场景、关键词、人群的消耗、投产、点击、转化、引潜和加购成本。

截图里的 BI 编辑流大致是：

`来源表 -> 字段设置/计算字段 -> 上下合并 -> 分类汇总 -> 排序 -> 图表组件或明细表`

Web 端建议只做“展示+筛选+导入”，不要把 BI 拖拽编辑器照搬出来。前台呈现固定的分析视图，后台把这些聚合逻辑固化成数据服务。

## 2. 源数据与表关系

| 源文件 | BI 来源表名 | 编码/Sheet | 日期字段 | 原始行数 | 作用 |
| --- | --- | --- | --- | ---: | --- |
| `生参商品排行榜.xlsx` | `店铺数据_商品维度` | `商品维度` | `统计日期` | 7,020 | 商品经营数据、商品维度分析主表 |
| `推广商品报表_20260118_130730.csv` | `店铺数据_推广商品报表` | GB18030 | `日期` | 16,830 | 货品、计划、场景投放数据 |
| `推广内容报表_20260118_130815.csv` | `店铺数据_推广内容报表` | GB18030 | `日期` | 14,871 | 短视频/内容投放数据 |
| `推广关键词报表_20260118_130845.csv` | `店铺数据_推广关键词报表` | GB18030 | `日期` | 24,646 | 关键词/词包投放数据 |
| `推广人群报表_20260118_130755.csv` | `店铺数据_推广人群报表` | GB18030 | `日期` | 143,634 | 人群、货品全站推、短视频等人群归因数据 |

当前源数据日期范围：

- 推广 CSV：`2025-10-20` 至 `2026-01-17`
- 商品生参：`2025-10-21` 至 `2026-01-17`

全局筛选参数：

- 顶部 `日期区间` 默认“无限制”。
- 对推广表使用 `日期` 过滤。
- 对商品维度使用 `统计日期` 过滤。

## 3. 字段清洗与标准字段

### 3.1 编码字段

这些字段在截图中是 BI 的计算字段，Web 端需要在导入后统一生成。

| 计算字段 | 来源 | 公式/拼接逻辑 | 用途 |
| --- | --- | --- | --- |
| `主体编码` | 商品维度 | `商品ID + 商品标题` | 商品维度树图、商品明细表 |
| `主体编码` | 推广商品/推广内容 | `主体ID + 主体名称` | 推广商品树图 |
| `计划编码` | 推广商品/推广内容 | `计划名字 + 主体名称` | 推广商品计划明细表 |
| `关键词编码` | 推广关键词 | `词类型 + 词名字/词包名字 + 宝贝名称` | 关键词明细表 |
| `人群编码` | 推广人群 | `人群名字 + 场景名字 + 单元名字` | 人群明细表；按花费大于 0 后为 4,919 行 |
| `内容编码` | 推广内容 | `主体类型 + 主体名称` | 内容分析表 |
| `日期周` | 推广商品/内容 | ISO 周，形如 `2026-03 (01/12-01/18)` | 周趋势横向指标图 |

### 3.2 上下合并逻辑

截图里的 `上下合并` 对应的是 Union All，不是 Join。

最重要的一处：

`推广商品分析表 = 推广商品报表 UNION ALL 推广内容报表`

原因是推广商品图表中有 `超级短视频` 场景，而这个场景只存在于推广内容报表。合并后场景汇总能对上截图：

| 场景名字 | 花费 | 投产 | 引潜比 |
| --- | ---: | ---: | ---: |
| 人群推广 | 1,577,536.69 | 7.76 | 37.15% |
| 关键词推广 | 3,094,550.84 | 5.59 | 51.58% |
| 货品全站推 | 1,584,472.11 | 10.29 | 50.80% |
| 超级短视频 | 944,420.29 | 9.60 | 29.70% |

## 4. 核心指标口径

重要原则：**比率类指标都用“先汇总分子和分母，再相除”，不要平均原始行里的比率。**

### 4.1 商品经营指标

| 指标 | 公式 |
| --- | --- |
| `支付金额` | `SUM(支付金额)` |
| `商品访客数` | `SUM(商品访客数)` |
| `转化率` | `SUM(支付买家数) / SUM(商品访客数)` |
| `客单价` | `SUM(支付金额) / SUM(支付买家数)` |
| `年累计支付金额` | `MAX(年累计支付金额)`，不是 SUM |
| `年累计支付金额占比` | `SUM(支付金额) / MAX(年累计支付金额)` |
| `费比` | `SUM(推广消耗) / SUM(支付金额)` |
| `退款金额占比` | `SUM(成功退款金额) / SUM(支付金额)` |
| `复购率` | `SUM(支付老买家数) / SUM(支付买家数)` |
| `复购金额占比` | `SUM(老买家支付金额) / SUM(支付金额)` |
| `加购率` | `SUM(商品加购人数) / SUM(商品访客数)` |
| `人均浏览量` | `SUM(商品浏览量) / SUM(商品访客数)` |
| `链接净ROI` | `(SUM(支付金额) - SUM(成功退款金额)) / SUM(推广消耗)` |

示例校验：商品维度第一名 `593063365092...` 的支付金额为 `22,977,444.81`，占总支付金额 `17.97%`，费比 `10.11%`，与截图一致。

### 4.2 推广投放指标

| 指标 | 公式 |
| --- | --- |
| `花费` | `SUM(花费)` |
| `总成交金额` | `SUM(总成交金额)` |
| `投产` | `SUM(总成交金额) / SUM(花费)` |
| `点击单价` | `SUM(花费) / SUM(点击量)` |
| `点击率` | `SUM(点击量) / SUM(展现量)` |
| `转化率` | `SUM(总成交笔数) / SUM(点击量)` |
| `客单价` | `SUM(总成交金额) / SUM(总成交笔数)` |
| `间接转化率` | `SUM(间接成交笔数) / SUM(点击量)` |
| `加购成本` | `SUM(花费) / SUM(总购物车数)` |
| `引潜比` | `SUM(引导访问潜客数) / SUM(引导访问人数)` |
| `千次展现成本` | `SUM(花费) / SUM(展现量) * 1000` |

关键词表里的 `平均展现排名` 截图显示值接近 `SUM(平均展现排名) / SUM(展现量)`。这个能复刻原 BI，但业务上更建议改成 `SUM(平均展现排名 * 展现量) / SUM(展现量)`，作为加权平均排名。

### 4.3 空值与异常处理

源表中部分数字列会出现空值、文本、`#VALUE!` 风格异常。Web 端建议：

- 导入时把数字字段统一转为 number，无法转换的记为 `null`。
- 分母为 0 或空时，指标返回 `null`，前端显示 `-`。
- 百分比统一显示 2 位小数；金额可显示 2 位小数；投产显示 2 位小数。

## 5. 视图逻辑

### 5.1 商品维度分析表

来源：

- `店铺数据_商品维度`

聚合维度：

- `主体编码 = 商品ID + 商品标题`

聚合后行数：

- 132 行。源数据里存在若干商品 ID/标题为空且支付为 0 的行，Web 端可默认隐藏空主体。

图表 1：商品树图

| 配置项 | 口径 |
| --- | --- |
| 图表类型 | 矩形树图 / Treemap |
| 面积大小 | `支付金额` |
| 分组/标签 | `主体编码` |
| 标签内容 | `主体编码`、`费比`、`支付金额`、`支付金额占比` |
| Tooltip | `主体编码`、`费比`、`支付金额`、`占比` |
| 排序 | `支付金额` 降序 |

图表 2：商品明细表

字段顺序建议：

1. `主体编码`
2. `支付金额`
3. `商品访客数`
4. `转化率`
5. `客单价`
6. `年累计支付金额`
7. `年累计支付金额占比`
8. `费比`
9. `退款金额占比`
10. `复购率`
11. `复购金额占比`
12. `加购率`
13. `人均浏览量`
14. `链接净ROI`

默认排序：

- `支付金额` 降序。

业务用途：

- 找主销商品：看支付金额、占比。
- 找效率商品：看费比、链接净 ROI、退款金额占比。
- 找复购商品：看复购率、复购金额占比。
- 找种草潜力：看加购率、人均浏览量。

### 5.2 推广商品分析表

来源：

- `推广商品报表 UNION ALL 推广内容报表`

这是最重要的投放总览页。它把商品计划、货品全站推、关键词、人群、短视频统一到一个投放视角里。

图表 1：推广主体树图

| 配置项 | 口径 |
| --- | --- |
| 维度 | `主体编码 = 主体ID + 主体名称` |
| 面积大小 | `花费` |
| 标签内容 | `主体编码`、`投产`、`花费`、`花费占比` |
| Tooltip | `主体编码`、`投产`、`花费`、`占比` |
| 排序 | `花费` 降序 |

截图第一名复核：

- `593063365092WHC小千金...`
- 花费 `2,576,397.58`
- 投产 `6.36`
- 花费占比 `35.78%`

图表 2：场景柱线图

| 配置项 | 口径 |
| --- | --- |
| X 轴 | `场景名字` |
| 左轴柱形 | `花费` |
| 右轴折线 | `投产`、`引潜比` |
| 数据来源 | 推广商品 + 推广内容合并后，按场景汇总 |

图表 3：周趋势指标矩阵

| 配置项 | 口径 |
| --- | --- |
| 纵轴 | `日期周`，最新周在上 |
| 横向指标 | `花费`、`投产`、`点击单价`、`点击率`、`转化率`、`客单价`、`间接转化率`、`引潜比`、`加购成本`、`千次展现成本` |
| 图形 | 每个指标一列横向条形图，独立刻度 |

示例校验：

- `2026-03 (01/12-01/18)` 花费 `354,769.27`、投产 `5.97`、点击单价 `2.09`、点击率 `2.76%`、转化率 `2.25%`、客单价 `554.05`，与截图一致。

图表 4：推广计划明细表

聚合维度：

- `计划编码 = 计划名字 + 主体名称`

聚合后行数：

- 1,719 行。

字段顺序：

1. `计划编码`
2. `花费`
3. `总成交金额`
4. `投产`
5. `点击单价`
6. `转化率`
7. `客单价`
8. `引潜比`
9. `加购成本`

默认排序：

- `花费` 降序。

### 5.3 推广关键词分析表

来源：

- `推广关键词报表`

聚合维度：

- `关键词编码 = 词类型 + 词名字/词包名字 + 宝贝名称`

明细聚合：

- 全部关键词编码 1,031 行。
- 花费大于 0 的关键词编码 822 行。

图表建议：

| 图表 | 口径 |
| --- | --- |
| 关键词词云 | 按 `词名字/词包名字` 汇总，权重用 `花费`，颜色区分 `词类型` |
| 词类型饼图 | 按 `词类型` 汇总 `花费`；截图中关键词约 `88.69%`，关键词包约 `11.31%` |
| 关键词气泡图 | 从截图推断：X 轴可用 `转化率`，Y 轴用 `花费`，气泡大小用 `总成交金额`，标签用 `词名字/词包名字` |
| 关键词明细表 | 按 `关键词编码` 展示花费、投产、点击单价、转化率、客单价、引潜比、平均展现排名、加购成本 |

默认排序：

- `花费` 降序。

业务用途：

- 花费高但投产低：要降价/否词/收窄匹配。
- 转化率高但花费低：可扩量。
- 引潜比高：适合种草和拉新，不能只用 ROI 判断。

### 5.4 推广人群分析表

来源：

- `推广人群报表`

聚合维度：

- `人群编码 = 人群名字 + 场景名字 + 单元名字`

聚合后口径：

- 按上述维度汇总后，`花费 > 0` 的人群编码为 4,919 行，和截图页脚一致。

图表建议：

| 图表 | 口径 |
| --- | --- |
| 场景词云 | 按 `场景名字` 汇总 `花费`，突出人群推广、货品全站推、超级短视频、关键词推广 |
| 人群词云 | 按 `人群编码` 或 `人群名字` 汇总 `花费`，展示主要消耗人群 |
| 人群明细表 | 按 `人群编码` 展示花费、投产、点击单价、转化率、客单价、引潜比、加购成本 |

默认排序：

- `花费` 降序。

业务用途：

- 找高消耗人群：优先看花费、投产、加购成本。
- 找高价值人群：看投产、转化率、客单价。
- 找潜客人群：看引潜比、点击率、加购率。

### 5.5 推广内容分析表

来源：

- `推广内容报表`

这个视图截图中没有展开，但左侧导航存在，且它的数据被推广商品分析表合并使用。

建议 Web 端保留独立页：

| 图表/表格 | 口径 |
| --- | --- |
| 内容树图 | `内容编码 = 主体类型 + 主体名称`，面积用 `花费`，颜色用 `投产` |
| 内容明细表 | 按 `主体ID + 主体名称` 或 `计划名字 + 主体名称` 汇总 |
| 场景趋势 | 目前主要是 `超级短视频`，可按周展示花费、投产、点击率、转化率 |

内容表聚合参考：

- `主体ID + 主体名称`：300 行，花费大于 0 为 285 行。
- `计划名字 + 主体名称`：598 行，可用于计划内容明细。

## 6. Web 端信息架构

### 6.1 页面导航

建议左侧导航保留两组：

源数据：

- 商品维度源表
- 推广商品源表
- 推广内容源表
- 推广关键词源表
- 推广人群源表

分析视图：

- 商品维度分析表
- 推广商品分析表
- 推广关键词分析表
- 推广人群分析表
- 推广内容分析表

### 6.2 顶部参数

全局参数：

- 日期区间
- 店铺名称
- 场景名字
- 商品/主体搜索
- 花费阈值
- 投产阈值

每个页面保留：

- 搜索框
- 指标排序
- 导出 CSV/XLSX
- 明细表分页

### 6.3 页面风格

截图展示态是深色主题，编辑态是浅色 BI 后台。Web 端建议：

- 面向老板/运营复盘的展示页：深色主题，突出图表和关键数。
- 面向日常操作的管理页：浅色主题，便于导入、筛选、检查源数据。

## 7. 建议的数据服务设计

### 7.1 数据表

原始层：

- `raw_product_daily`
- `raw_ad_item_daily`
- `raw_ad_content_daily`
- `raw_ad_keyword_daily`
- `raw_ad_crowd_daily`

汇总层：

- `mart_product_subject`
- `mart_ad_subject`
- `mart_ad_plan_subject`
- `mart_ad_scene`
- `mart_ad_weekly`
- `mart_keyword`
- `mart_crowd`
- `mart_content`

### 7.2 API 口径

| API | 用途 |
| --- | --- |
| `POST /api/import` | 上传 xlsx/csv，自动识别表类型 |
| `GET /api/datasets` | 源表状态、行数、日期范围 |
| `GET /api/views/product` | 商品维度树图+表格 |
| `GET /api/views/ad-products` | 推广商品页所有图表 |
| `GET /api/views/keywords` | 关键词页所有图表 |
| `GET /api/views/crowds` | 人群页所有图表 |
| `GET /api/views/contents` | 内容页所有图表 |
| `GET /api/export?view=...` | 导出当前筛选后的明细 |

### 7.3 技术选型建议

适合快速落地的组合：

- 前端：React/Next.js + ECharts
- 后端：FastAPI
- 计算层：DuckDB 或 SQLite
- 文件解析：CSV 使用 GB18030 解码，XLSX 使用 pandas/openpyxl

原因：

- 数据量不大，单机数据库足够。
- ECharts 很适合复刻树图、柱线图、词云、气泡图、横向多指标条形图。
- DuckDB 对 CSV/XLSX 导入和聚合查询很友好，后续数据量变大也能撑住。

## 8. 开发优先级

### P0：先复刻核心展示

1. 文件导入和表类型识别。
2. 日期筛选。
3. 商品维度分析表：树图 + 明细表。
4. 推广商品分析表：树图 + 场景柱线图 + 周趋势矩阵 + 明细表。

### P1：补齐投放诊断

1. 推广关键词分析表。
2. 推广人群分析表。
3. 推广内容分析表。
4. 导出当前视图。

### P2：运营决策能力

1. 高花费低投产预警。
2. 高转化低消耗机会词/人群。
3. 商品费比、退款、复购、加购综合评分。
4. 周环比变化与异常点标记。

## 9. 最容易踩坑的地方

1. 推广商品页必须合并推广商品和推广内容，否则会缺 `超级短视频`。
2. 商品维度的 `年累计支付金额` 要取最大值，不能把每日累计值求和。
3. `链接净ROI` 是扣退款后的净支付金额除以推广消耗。
4. 比率指标要用 ratio of sums，不能平均每天或每行的比率。
5. CSV 是 GB18030，不是 UTF-8。
6. 源表中有文本型数字和空值，Web 端要统一清洗，否则会复现 BI 里的 `#VALUE!`。
7. 周趋势图的周标签要按周一到周日生成；最后一周 `2026-03 (01/12-01/18)` 虽然源数据只到 01/17，也显示完整周区间。

