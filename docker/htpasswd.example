# 这是占位说明文件。真实的口令文件必须叫 docker/htpasswd（不带 .example），且不入库。
#
# 生成方式（任选一种）：
#
# 1) 服务器上已装 apache2-utils：
#      htpasswd -cb docker/htpasswd admin "你的强密码"
#      htpasswd -b  docker/htpasswd alice "她的密码"        # 追加第二个用户
#
# 2) 没装 apache2-utils 时用 Docker 临时生成：
#      docker run --rm httpd:2.4-alpine htpasswd -nbB admin "你的强密码" > docker/htpasswd
#
# 注意：
# - 密码至少 12 位 + 大小写 + 数字 + 符号
# - 推荐使用 bcrypt（-B 参数），不要用默认的 crypt
# - 文件权限 chmod 600 docker/htpasswd
